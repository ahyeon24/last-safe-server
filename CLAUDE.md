# last-safe-server

막차 세이프(Last Safe) 백엔드 API 서버. 상세 스펙은 **`docs/spec.md`를 항상 먼저 읽고** 작업할 것.
원본 설계 문서(.docx)는 `docs/source/`에 있음 — spec.md에 없는 세부사항이 필요할 때만 참조.

## 프로젝트 목표

1. iOS 앱스토어 실제 출시
2. 백엔드 개발자 취업 포트폴리오 — 단순 CRUD/프록시가 아니라 도메인 로직(막차 역산 알고리즘,
   알람 스케줄링, 인증, 외부 API 통합, 캐싱 전략)을 제대로 구현하는 것이 핵심 목적.
   지름길로 기능을 흉내만 내지 말고, 왜 이렇게 설계했는지 설명 가능한 코드를 짤 것.

## 기술 스택

Java / Spring Boot / MySQL / Redis / Gradle

## 절대 위반하면 안 되는 설계 결정

이 프로젝트는 여러 차례 자체 비판 검토를 거쳐 아래 결정들을 확정했다. 코드가 이 결정과
다르게 짜여지려 하면 **먼저 알리고 확인받을 것** — 임의로 "더 나은 방법"이라며 되돌리지 말 것.

- **iOS 로컬 알림만 사용.** APNs 서버 푸시, 디바이스 푸시 토큰, 발송 재시도 로직 절대 구현하지 않음.
- **ODsay는 실시간 API 직접 호출 + Redis 캐싱.** 배치로 자체 DB를 구축하는 방식 금지
  (ODsay ToS상 데이터 저장 금지 조항 + 정확도 문제로 기각된 안).
- **candidate_id 기반 설계.** `/routes/search` 응답의 계산 결과는 서버가 Redis에
  candidate_id로 캐싱(TTL 10분)한다. 클라이언트가 계산값을 재전송해서 그대로 저장하는 방식 금지 —
  반드시 서버가 캐시에서 원본을 조회해서 저장해야 함 (클라이언트 제공값 무검증 신뢰 방지).
- **막차 시각 캐시(당일 자정 만료)와 candidate 캐시(TTL 10분)는 다른 캐시.** 혼동해서 하나로
  합치지 말 것.
- **막차 전멸 판정은 버퍼 적용 전 원본 시각 기준.** 순서를 바꾸면 실제로는 막차가 남았는데
  전멸로 오판할 수 있음.
- **경로 조회 이력은 알람 등록된 것만 DB 저장.** 검색만 하고 선택 안 한 후보는 영구 저장 안 함.
- **지도 SDK 기반 자체 내비게이션 구현 금지.** 카카오맵 등 외부 앱 딥링크로 위임 (FR-02d).
- **출발 시각 사용자 지정 기능 없음.** 서버는 항상 요청 수신 시각을 기준으로 계산.

## 막차 역산 알고리즘 구현 시 주의

`docs/spec.md` 5절 참고. 과거 v0.1에서 실제로 발생했던 버그 재발 방지:

1. 병목 인덱스(`bottleneck_segment_index`)는 `implied_by_next`가 채택될 때 갱신하지 말 것
   (이미 확정된 더 뒤쪽 구간의 병목이 진짜 병목).
2. 도보 구간 소요시간은 즉시 차감하지 말고 `pending_walk_sec`에 누적 후 다음 교통 구간에서 반영.
   경로가 도보로 끝나는 경우 순회 종료 후 남은 값을 최종 반영해야 함 — 빠뜨리기 쉬움.
3. `NO_SERVICE`(막차 데이터상 운행 없음)와 `FETCH_ERROR`(API/캐시 조회 실패)는 반드시 분리 처리.
   전자는 `NO_ROUTE_AVAILABLE`(200), 후자는 `EXTERNAL_API_UNAVAILABLE`(503).

## 작업 방식 — 바이브코딩

Java/Spring Boot 경험이 거의 없는 상태에서, **직접 짜면서 배우는 방식**으로 진행 중.
문법을 먼저 가르치고 코딩을 시작하는 순서가 아니라, 실제로 동작하는 걸 만들면서 그때그때
"이건 왜 이렇게 짜는지"를 이해해가는 흐름을 원함. 이에 맞춰 아래 원칙으로 작업할 것.

- **모든 커뮤니케이션은 한국어로.** 커밋 메시지, 코드 주석, PR 설명 포함.
- 기능은 막지 말고 일단 진행해서 돌아가게 만들 것. 대신 **커밋(또는 기능 단위)마다 짧게**
  "이 코드에서 이 부분이 왜 이렇게 되어있는지" 핵심만 짚어줄 것 — 장문 강의 X, 2~5줄 요약 O.
  Java/Spring 개념이 처음 등장하면(예: `@Transactional`, `@Service`, 의존성 주입) 한두 문장으로
  "이게 뭐고 왜 필요한지"만 짚고 넘어가기. 필요하면 더 물어볼 수 있게 여지를 남길 것.
- 큰 구조 변경(새 테이블, API 스펙 변경, 캐싱 전략 변경 등)은 진행 전에 트레이드오프를 간단히
  설명하고 확인받을 것. 다만 이건 "설계 결정"급 변경에 한하며, 일반적인 구현 세부사항까지
  매번 확인받을 필요는 없음 — 막히지 않고 진행하는 것이 우선.
- 문서(spec.md, SRS 등)와 실제 코드가 어긋나면 짚어줄 것 — 조용히 넘어가지 말 것.
- 에러가 나거나 막히면 원인을 먼저 설명하고 고칠 것. "일단 되게만 만들기" 식으로 원인 모른 채
  땜질하지 말 것 — 나중에 같은 문제 반복되면 아현님이 스스로 못 고침.

## 디렉토리 구조 (예정)

```
src/main/java/com/lastsafe/
  domain/          # 도메인 서비스 (LastTrainCalculationService 등)
  api/             # REST 컨트롤러
  external/        # ODsay, 서울시/국토부 API 연동 (인터페이스로 추상화, NFR-07)
  cache/           # Redis 캐싱 로직
  entity/          # JPA 엔티티 (ERD 기준)
docs/
  spec.md          # 핵심 스펙 요약 (항상 먼저 참조)
  source/          # 원본 .docx 설계 문서 + ERD
```
