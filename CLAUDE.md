# last-safe-server

막차 세이프(Last Safe) 백엔드 API 서버. 상세 스펙은 **`docs/spec.md`를 항상 먼저 읽고** 작업할 것.
원본 설계 문서(.docx)는 `docs/source/`에 있음 — spec.md에 없는 세부사항이 필요할 때만 참조.

## 프로젝트 목표

1. **iOS 앱스토어 실제 출시 및 운영.** 데모/포트폴리오용 껍데기가 아니라, 실제 사용자가 쓰고
   실제 개인정보(위치, OAuth 계정)를 다루는 서비스로 만드는 것이 최우선 목표.
2. 잘되면 사업화(창업) 고려 중 — 따라서 "일단 돌아가면 됨" 수준이 아니라, 나중에 사용자·트래픽이
   늘어도 무너지지 않을 기본기(보안, 비용 통제, 장애 대응)를 처음부터 염두에 두고 짤 것.
3. 백엔드 개발자 취업 포트폴리오로도 활용 — 단순 CRUD/프록시가 아니라 도메인 로직(막차 역산 알고리즘,
   알람 스케줄링, 인증, 외부 API 통합, 캐싱 전략)을 제대로 구현하는 것.
   지름길로 기능을 흉내만 내지 말고, 왜 이렇게 설계했는지 설명 가능한 코드를 짤 것.

**우선순위가 충돌할 때는 1·2번(실제 운영 가능한 수준)을 3번(포트폴리오 그럴싸함)보다 우선한다.**
예: "보여주기엔 충분하지만 보안 구멍이 있는 방식" vs "조금 더 손이 가지만 안전한 방식" 중에서는
후자를 택할 것.

## 기술 스택

Java / Spring Boot / MySQL / Redis / Gradle

## 절대 위반하면 안 되는 설계 결정

이 프로젝트는 여러 차례 자체 비판 검토를 거쳐 아래 결정들을 확정했다. 코드가 이 결정과
다르게 짜여지려 하면 **먼저 알리고 확인받을 것** — 임의로 "더 나은 방법"이라며 되돌리지 말 것.

- **iOS 로컬 알림만 사용.** APNs 서버 푸시, 디바이스 푸시 토큰, 발송 재시도 로직 절대 구현하지 않음.
- **ODsay는 실시간 API 직접 호출 + Redis 캐싱.** 배치로 자체 DB를 구축하는 방식 금지
  (ODsay ToS상 데이터 저장 금지 조항 + 정확도 문제로 기각된 안).
- **candidate_id 기반 설계.** `/routes/search` 응답의 계산 결과는 서버가 Redis에
  candidate_id로 캐싱(TTL 10분)한다. 클라이언트가 계산값을 재전송해서 그대로 저장하는 방식 금지 —
  반드시 서버가 캐시에서 원본을 조회해서 저장해야 함 (클라이언트 제공값 무검증 신뢰 방지).
- **막차 시각 캐시(당일 자정 만료)와 candidate 캐시(TTL 10분)는 다른 캐시.** 혼동해서 하나로
  합치지 말 것.
- **막차 전멸 판정은 버퍼 적용 전 원본 시각 기준.** 순서를 바꾸면 실제로는 막차가 남았는데
  전멸로 오판할 수 있음.
- **경로 조회 이력은 알람 등록된 것만 DB 저장.** 검색만 하고 선택 안 한 후보는 영구 저장 안 함.
- **지도 SDK 기반 자체 내비게이션 구현 금지.** 카카오맵 등 외부 앱 딥링크로 위임 (FR-02d).
- **출발 시각 사용자 지정 기능 없음.** 서버는 항상 요청 수신 시각을 기준으로 계산.

## 보안 원칙 (실사용자 서비스이므로 필수)

포트폴리오용 데모라면 생략해도 될 것들이지만, 실제 사용자의 위치·계정 정보를 다루는 서비스이므로
처음부터 지킬 것. 나중에 "보안 리팩토링"으로 몰아서 하지 말 것 — 뚫리고 나서 고치는 건 늦음.

**이 섹션에 해당하는 코드는 "작업 방식 — 바이브코딩" 절의 일반 원칙이 아니라, 그 절 안의
"예외: 보안 관련 코드" 기준을 따를 것** (속도보다 정확성, 진행 전 설명·확인).

- **API 키·DB 비밀번호 등은 절대 코드/커밋에 하드코딩 금지.** 환경변수로 관리 (NFR-06).
  `.env`, `application-local.yml` 등 민감정보 파일은 `.gitignore`에 반드시 포함하고, 실수로
  커밋됐는지 매 커밋 전 확인할 것.
- **OAuth `provider_token`은 반드시 서버에서 검증.** 클라이언트가 보낸 값을 그대로 신뢰하지 말고
  Apple/Kakao의 공식 토큰 검증 API를 통해 서버가 직접 확인.
- **access_token은 무기한 유효 금지.** 만료 시간을 두고, 필요하면 리프레시 토큰 방식 고려.
  회원 탈퇴 시 즉시 전체 무효화 (API 명세서 7.1절 기준 이미 확정된 요구사항).
- **타인 리소스 접근 방지(403 FORBIDDEN)를 예외적으로 빼먹지 말 것.** 알람/즐겨찾기 수정·삭제
  시 소유자 확인은 매번, 빠짐없이 — 이건 "나중에 추가"가 아니라 처음부터 있어야 함.
- **위치 정보(출발지/목적지 좌표)는 민감정보로 취급.** 로그에 평문으로 남기지 않도록 주의.
- **SQL 인젝션 방지**는 JPA/파라미터 바인딩 사용으로 기본 방어되지만, 네이티브 쿼리를 쓸 일이
  생기면 반드시 파라미터 바인딩 방식으로.

## 비용 관리 (외부 API·인프라 비용을 실제로 내가 부담함)

포트폴리오와 달리 실제 서비스는 트래픽이 늘면 비용이 실제로 청구된다. 설계 시 비용 관점도 고려할 것.

- **ODsay Basic(무료) 등급은 일 1,000회 제한.** Redis 캐싱(막차 시각 캐시 = 당일 자정 만료,
  candidate 캐시 = TTL 10분)은 정확도뿐 아니라 **API 호출량을 줄여 비용/한도를 지키기 위한
  장치이기도 함**을 인지하고 구현할 것. 캐시 히트율이 낮아지는 코드 변경은 특히 주의.
- 사용자가 늘어나서 무료 등급 한도를 넘길 가능성이 보이면, 조용히 넘어가지 말고 미리 알릴 것
  (유료 전환 시점, 예상 비용 등).
- 서버 호스팅(추후 배포 시)도 트래픽 기반 과금이 될 수 있으므로, 불필요하게 리소스를
  낭비하는 코드(예: 불필요한 반복 쿼리, N+1 문제)는 "일단 되니까 넘어가기"로 두지 말 것.

## 막차 역산 알고리즘 구현 시 주의

`docs/spec.md` 5절 참고. 과거 v0.1에서 실제로 발생했던 버그 재발 방지:

1. 병목 인덱스(`bottleneck_segment_index`)는 `implied_by_next`가 채택될 때 갱신하지 말 것
   (이미 확정된 더 뒤쪽 구간의 병목이 진짜 병목).
2. 도보 구간 소요시간은 즉시 차감하지 말고 `pending_walk_sec`에 누적 후 다음 교통 구간에서 반영.
   경로가 도보로 끝나는 경우 순회 종료 후 남은 값을 최종 반영해야 함 — 빠뜨리기 쉬움.
3. `NO_SERVICE`(막차 데이터상 운행 없음)와 `FETCH_ERROR`(API/캐시 조회 실패)는 반드시 분리 처리.
   전자는 `NO_ROUTE_AVAILABLE`(200), 후자는 `EXTERNAL_API_UNAVAILABLE`(503).

## 작업 방식 — 바이브코딩

Java/Spring Boot 경험이 거의 없는 상태에서, **직접 짜면서 배우는 방식**으로 진행 중.
문법을 먼저 가르치고 코딩을 시작하는 순서가 아니라, 실제로 동작하는 걸 만들면서 그때그때
"이건 왜 이렇게 짜는지"를 이해해가는 흐름을 원함. 이에 맞춰 아래 원칙으로 작업할 것.

**단, 이 방식은 일반 기능 구현에 한한다. 아래 "보안 관련 코드"에는 다른 기준을 적용할 것
(바로 다음 절 참고) — 속도보다 정확성이 우선이며, 이 경우엔 예외로 취급한다.**

- **모든 커뮤니케이션은 한국어로.** 커밋 메시지, 코드 주석, PR 설명 포함.
- 기능은 막지 말고 일단 진행해서 돌아가게 만들 것. 대신 **커밋(또는 기능 단위)마다 짧게**
  "이 코드에서 이 부분이 왜 이렇게 되어있는지" 핵심만 짚어줄 것 — 장문 강의 X, 2~5줄 요약 O.
  Java/Spring 개념이 처음 등장하면(예: `@Transactional`, `@Service`, 의존성 주입) 한두 문장으로
  "이게 뭐고 왜 필요한지"만 짚고 넘어가기. 필요하면 더 물어볼 수 있게 여지를 남길 것.
- 큰 구조 변경(새 테이블, API 스펙 변경, 캐싱 전략 변경 등)은 진행 전에 트레이드오프를 간단히
  설명하고 확인받을 것. 다만 이건 "설계 결정"급 변경에 한하며, 일반적인 구현 세부사항까지
  매번 확인받을 필요는 없음 — 막히지 않고 진행하는 것이 우선.
- 문서(spec.md, SRS 등)와 실제 코드가 어긋나면 짚어줄 것 — 조용히 넘어가지 말 것.
- 에러가 나거나 막히면 원인을 먼저 설명하고 고칠 것. "일단 되게만 만들기" 식으로 원인 모른 채
  땜질하지 말 것 — 나중에 같은 문제 반복되면 아현님이 스스로 못 고침.

### 예외: 보안 관련 코드는 속도보다 정확성 우선

아래에 해당하는 코드는 위의 "막지 말고 일단 진행" 원칙을 적용하지 않는다. 빠르게 짜고 넘어가지
말고, **왜 이렇게 짜야 안전한지 먼저 설명하고, 아현님이 이해했는지 확인한 뒤 진행**할 것.
바이브코딩 흐름을 깨더라도 이 영역은 예외로 둔다.

- **인증/인가**: OAuth 토큰 검증, `access_token` 발급·검증·만료 처리, 회원 탈퇴 시 토큰 무효화
- **권한 체크**: 알람/즐겨찾기 등 본인 소유 리소스 확인 로직 (403 FORBIDDEN 분기)
- **시크릿 관리**: API 키, DB 비밀번호, 환경변수 설정 방식
- **사용자 입력 검증**: 특히 DB 쿼리에 사용자 입력값이 들어가는 지점 (SQL 인젝션 등)
- **개인정보(위치 좌표 등)가 로그·응답에 노출되는지 여부**

이 영역에서 "일단 되니까 넘어가자"는 판단이 들면, 그 판단 자체를 아현님께 먼저 알릴 것.

## 실제 출시 전 반드시 처리해야 할 것 (지금 당장은 아니어도 잊지 말 것)

MVP 코딩을 막지 않기 위해 지금 당장 다루지는 않지만, **실제 앱스토어 출시 전에는 반드시
확정·구현해야** 하는 항목들. SRS에는 "미해결/향후 검토"로만 남아있어서 미루기 쉬우니 여기 남겨둠.
이 항목들을 "그냥 넘어가도 되는 문서상 각주" 취급하지 말 것.

- **서울시 정류소별 노선 첫차/막차 API 실제 가용 여부 검증.** 문서상 "서비스 종료 안내"가 표시된
  상태 (SRS 5.1절). 이게 서울 지역 버스 막차 정확도의 핵심 데이터 소스인데 아직 미검증 —
  실제 API 키 발급받아서 최대한 빨리 확인할 것. 안 되면 국토부 API 추정 방식으로 서울도 대체해야
  할 수 있음 (설계 영향 큼, 조기 확인 필요).
- **개인정보처리방침, 이용약관 작성 및 앱 내 게시.** 위치정보, OAuth 계정 정보를 다루므로 앱스토어
  심사에서도 요구되고 법적으로도 필요.
- **위치기반서비스 사업자 신고 여부 확인.** 국내에서 위치정보를 활용한 서비스는 방송통신위원회
  신고 대상일 수 있음 — 실제 출시 전 법적 요건 확인 필요.
- **개인정보 보존/삭제 정책 확정** (SRS 5.3절 미해결 사항). FR-15a 회원 탈퇴 시 삭제 범위와
  기간을 실제 법령 기준으로 확정하고 구현.
- **최소한의 로깅·모니터링·장애 알림 체계.** 실사용자가 있는데 서버가 죽어도 개발자가 모르는
  상황은 피할 것. 처음부터 정교할 필요는 없지만, 최소한 "장애 났을 때 인지는 가능한" 수준은
  MVP 이후 우선순위로 둘 것.
- **막차 계산 로직에 대한 테스트 코드.** 실사용자가 이 계산을 믿고 막차를 타러 나가는 서비스이므로,
  알고리즘 설계서 6절의 검증 예시를 최소한 단위 테스트로 옮겨둘 것 — 리팩토링 시 회귀 방지.

## 디렉토리 구조 (예정)

```
src/main/java/com/lastsafe/
  domain/          # 도메인 서비스 (LastTrainCalculationService 등)
  api/             # REST 컨트롤러
  external/        # ODsay, 서울시/국토부 API 연동 (인터페이스로 추상화, NFR-07)
  cache/           # Redis 캐싱 로직
  entity/          # JPA 엔티티 (ERD 기준)
docs/
  spec.md          # 핵심 스펙 요약 (항상 먼저 참조)
  source/          # 원본 .docx 설계 문서 + ERD
```
